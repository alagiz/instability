version: '3.2'
services:
  client-app:
    image: alagiz/instability_client-app:latest
    environment:
      - BACKEND_IP=${HOST_IP}:8080
    command: bash -c "nginx -g \"daemon off;\""
    ports:
      - "80:80"
